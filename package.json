{
  "name": "setup-swiftsyntax-prebuilts",
  "version": "1.0.0",
  "description": "GitHub Action to build and cache prebuilt SwiftSyntax for nightly toolchains",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/cache": "^4.0.0",
    "@actions/core": "^1.11.0",
    "@actions/exec": "^1.1.1",
    "@peculiar/x509": "^1.12.0",
    "jose": "^5.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.5.0"
  }
}
